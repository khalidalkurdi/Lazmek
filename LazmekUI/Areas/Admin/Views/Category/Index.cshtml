
@model List<Category>
<div class="card mt-4 border-0 shadow bg-dark-mode text-light">
    <div class="card-header bg-gradient py-3 ms-0" style="background-color:goldenrod">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class=" text-white py-2"> Category List</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row py-3">
            <div class="col-12 text-end">
                <a class="btn btn-primary" asp-controller="Category" asp-action="Create"> <i class="bi bi-plus-circle"></i> Add New Category </a>
            </div>
        </div>
        <table class=" table table-bordered table-striped table-dark">
            <thead>
                <tr>
                    <th>Name</th>                    
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var Category in Model)
                {
                    <tr>
                        <td>@Category.Name</td>                        
                        <td>
                            <div class="w-75 btn-group ">
                                <a class="btn btn-danger" asp-controller="Category" asp-action="Delete" asp-route-id="@Category.Id"><i class="bi bi-trash"></i></a>
                                <a class="btn btn-primary" asp-controller="Category" asp-action="Edit" asp-route-id="@Category.Id"><i class="bi bi-pencil-square"></i></a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>